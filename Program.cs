using System;

namespace copyingArrays
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] gosho = {5}; //array of intergers
            int[] georgy = (int[])gosho.Clone(); //Clones the above array but result is in Object type, we convert//
            //object type to 
            

        }
        
    }
}
